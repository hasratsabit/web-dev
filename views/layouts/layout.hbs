<!DOCTYPE html>
<html>
	<head>
		<meta charset="utf-8">
		<title>{{title}}</title>
	</head>
	<body>

		{{> header }}
		{{> hero }}
		{{> services }}
		{{> portfolio }}
		{{> testimonials}}
		{{> training }}
		{{> contact }}
		{{> footer }}
		{{{body}}}
		<script src="/build/bundle.js" charset="utf-8"></script>
	</body>
</html>
